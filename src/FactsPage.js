import React from "react";

class FactsPage extends React.Component {

    render() {
        return (
          <>
            <p>
                Eventually this will be the facts page. it is a bit empty. it needs more content.
            </p>
            {/* TODO: add stuff from the wireframe, above is filler */}
          </>
        );
      }
  };
  
  export default FactsPage;