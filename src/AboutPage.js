import React from "react";

class AboutPage extends React.Component {

    render() {
        return (
          <>
            <p>
                This is the about page. it is a bit empty. it needs more content.
            </p>
            {/* TODO: add stuff from the wireframe, above is filler */}
          </>
        );
      }
  };
  
  export default AboutPage;